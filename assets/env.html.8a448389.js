import{_ as e,o as a,c as d,e as o}from"./app.60df9e8e.js";const c={},i=o('<h1 id="环境变量" tabindex="-1"><a class="header-anchor" href="#环境变量" aria-hidden="true">#</a> 环境变量</h1><p>V2Ray 提供以下环境变量以供修改 V2Ray 的一些底层配置。</p><h2 id="每个连接的缓存大小" tabindex="-1"><a class="header-anchor" href="#每个连接的缓存大小" aria-hidden="true">#</a> 每个连接的缓存大小</h2><ul><li>名称：<code>v2ray.ray.buffer.size</code> 或 <code>V2RAY_RAY_BUFFER_SIZE</code>。</li><li>单位：MBytes。</li><li>默认值：在 x86、amd64、arm64 和 s390x 上为 2，其它平台上禁用该缓存。</li><li>特殊值：0 表示缓存无上限。</li></ul><p><strong>已过时，请使用本地策略中的 bufferSize</strong></p><p>对于一个代理连接，当上下游网络速度有差距时，V2Ray 会缓存一部分数据，以减小对网络传输的影响。这个配置设置了缓存的大小，越大的缓存会占用更多的内存，也会使网络性能越好。</p><h2 id="资源文件路径" tabindex="-1"><a class="header-anchor" href="#资源文件路径" aria-hidden="true">#</a> 资源文件路径</h2><ul><li>名称：<code>v2ray.location.asset</code> 或 <code>V2RAY_LOCATION_ASSET</code>。</li><li>默认值：和 v2ray 文件同路径。</li></ul><p>这个环境变量指定了一个文件夹位置，这个文件夹应当包含 geoip.dat 和 geosite.dat 文件。</p><h2 id="配置文件位置" tabindex="-1"><a class="header-anchor" href="#配置文件位置" aria-hidden="true">#</a> 配置文件位置</h2><ul><li>名称：<code>v2ray.location.config</code> 或 <code>V2RAY_LOCATION_CONFIG</code>。</li><li>默认值：和 v2ray 文件同路径。</li></ul><p>这个环境变量指定了一个文件夹位置，这个文件夹应当包含 config.json 文件。</p><h2 id="多配置目录" tabindex="-1"><a class="header-anchor" href="#多配置目录" aria-hidden="true">#</a> 多配置目录</h2><ul><li>名称：<code>v2ray.location.confdir</code> 或 <code>V2RAY_LOCATION_CONFDIR</code>。</li><li>默认值：<code>&quot;&quot;</code>。</li></ul><p>V2Ray 4.23.0 开始增加的多配置文件目录项。这个目录内的 <code>.json</code> 文件会按文件名顺序读取，作为多配置选项。</p><h2 id="分散读取" tabindex="-1"><a class="header-anchor" href="#分散读取" aria-hidden="true">#</a> 分散读取</h2><ul><li>名称：<code>v2ray.buf.readv</code> 或 <code>V2RAY_BUF_READV</code>。</li><li>默认值：<code>auto</code>。</li></ul><p>V2Ray 3.37 开始使用 Scatter/Gather IO，这一特性可以在大流量（超过 100 MByte/s）的时候依然使用较低的内存。可选的值有 <code>auto</code>、<code>enable</code> 和 <code>disable</code>。</p><ul><li><code>enable</code>：强制开启分散读取特性。</li><li><code>disable</code>：强制关闭分散读取特性。</li><li><code>auto</code>：仅在 Windows、macOS 和 Linux，并且 CPU 平台为 x86、AMD64 和 s390x 时，开启此特性。</li></ul><p>在流量没有达到 100 MByte/s 时，开启与否在内存使用上没有明显的差异。</p><h2 id="geodata-文件加载器" tabindex="-1"><a class="header-anchor" href="#geodata-文件加载器" aria-hidden="true">#</a> Geodata 文件加载器</h2><ul><li>名称：<code>v2ray.conf.geoloader</code> 或 <code>V2RAY_CONF_GEOLOADER</code>。</li><li>默认值：<code>standard</code>。</li></ul><p>自 v4.39.0 起，V2Ray 内置了多种读取和解码 <code>geoip.dat</code> 和 <code>geosite.dat</code> 文件的加载器。可选的加载器如下：</p><ul><li><code>standard</code>：标准加载器（默认值）</li><li><code>memconservative</code>：专为内存受限（小内存）设备优化的加载器 (v4.39.0+)</li></ul>',24),l=[i];function r(t,n){return a(),d("div",null,l)}const s=e(c,[["render",r],["__file","env.html.vue"]]);export{s as default};
